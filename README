# LaTeX Buchprojekt (Deutsch & Latein) mit Docker

## Build mit Docker
```bash
make docker-run

make inside
```

## Output
- PDF: `output/main.pdf`
- EPUB: `output/book.epub` oder `output/book-pandoc.epub`
